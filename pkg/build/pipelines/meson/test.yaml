name: Test project with meson

needs:
  packages:
    - meson

inputs:
  output-dir:
    description: |
      The output directory for the Meson test.
    default: output

pipeline:
  - runs: |
      meson test -C ${{inputs.output-dir}}
