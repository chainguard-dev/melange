name: Install a Perl package downloaded from cpan

needs:
  packages:
    - busybox
    - perl

pipeline:
  - runs: |
      export CFLAGS=$(perl -MConfig -E 'say $Config{ccflags}')
      PERL_MM_USE_DEFAULT=1 perl -I. Makefile.PL INSTALLDIRS=vendor
